# Config

This starter repo currently builds the `settings_reset` shield for `nice_nano`.

Add your actual keyboard's `.keymap`, `.conf`, and any custom shield files here
once the keyboard matrix and shield details are known.
